echo /usr/local/lib/pkgconfig to see where it points to
if it doesn't show anything, or the directory doesn't contain opencv.pc, then we need to change the path to /usr/local/lib/pkg_config
export $PKG_CONFIG_PATH=/usr/local/lib/pkg_config
